services:
  front:
    build:
      context: ./front
      dockerfile: Dockerfile
    ports:
      - "5174:5173"
    volumes:
      - ./front:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
