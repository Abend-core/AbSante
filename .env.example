# Production (docker-compose.prod.yml). Copier en `.env` (ignoré par git) et remplir.

# Mot de passe de la base PostgreSQL. Générer avec : openssl rand -hex 24
POSTGRES_PASSWORD=

# Nom de domaine public de l'application (défaut : absante.rxdy.fr).
# ABSANTE_HOST=absante.rxdy.fr
